# Copy this file to ".env" (same folder) and fill in your real PostgreSQL values.
# The .env file is loaded by the scripts and must NEVER be committed to git.

DB_HOST=localhost
DB_PORT=5432
DB_NAME=ecommerce
DB_USER=postgres
DB_PASSWORD=your_password
